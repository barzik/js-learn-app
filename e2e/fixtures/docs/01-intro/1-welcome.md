# Welcome

This is the welcome page used as a deterministic fixture for end-to-end tests.

It contains a paragraph, a list, and a fenced code block so the renderer is exercised end-to-end.

- one
- two
- three

```js
const greeting = 'hello';
console.log(greeting);
```
